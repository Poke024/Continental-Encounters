<Window
    x:Class="Continental_Encounters.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Continental_Encounters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="GridMain" Background="Black">
        <Grid.Resources>
            <DataTemplate x:Key="ZoneListViewTemplate" x:DataType="local:Zone">
                <TextBlock Text="{x:Bind _Name}" x:Phase="1" Margin="0,5,0,5"></TextBlock>
            </DataTemplate>
        </Grid.Resources>
        <RelativePanel>
            <MenuBar x:Name="Menu" HorizontalAlignment="Left" VerticalContentAlignment="Top" Height="Auto" Width="{Binding ElementName=GridMain,Path=ActualWidth}" Background="Gray">
                <MenuBar.Items>
                    <MenuBarItem Title="File" Foreground="Black"></MenuBarItem>
                    <MenuBarItem Title="Edit" Foreground="Black"></MenuBarItem>
                    <MenuBarItem Title="View" Foreground="Black"></MenuBarItem>
                    <MenuBarItem Title="Help" Foreground="Black"></MenuBarItem>
                </MenuBar.Items>
            </MenuBar>

            <Border BorderThickness='1' BorderBrush='Gray' RelativePanel.Below="Menu">
                <StackPanel x:Name="Zones" Orientation="Vertical" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <TextBlock x:Name="ZoneText" Margin="5, 5, 5, 0"  VerticalAlignment="Center">Zones</TextBlock>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" RelativePanel.Below="ZoneText">
                        <TextBox x:Name="ZoneName" VerticalContentAlignment="Center" Height="{Binding ElementName=remZone, Path=ActualHeight}"></TextBox>
                        <Button x:Name="newZone" HorizontalAlignment="Right" Margin="5, 5, 2, 5" Click="newZone_Click" Width="{Binding ElementName=remZone, Path=ActualWidth}">Add</Button>
                        <Button x:Name="remZone" HorizontalAlignment="Right" Margin="2, 5, 0, 5" Click="remZone_Click" Width="100">Remove</Button>
                    </StackPanel>
                    <ListView x:Name="ZoneList" ItemTemplate="{StaticResource ZoneListViewTemplate}" BorderThickness="1" Width="{Binding ElementName=ZoneText,Path=Width}" SelectionMode="Single" Background="Green"/>
                </StackPanel>
            </Border>

        </RelativePanel>
    </Grid>
</Window>
